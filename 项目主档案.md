# MyStartupProject1 项目方向与 BMad 前期调研建议

> 项目根目录约定：**Windows** `D:\work\MyStartupProject1`；**macOS** `/Users/stark/Desktop/work/MyStartupProject1`。下文 `<项目根目录>` 均指当前系统对应路径。

## 当前阶段

- 当前阶段：`功能开发` / `create-story` 入口（Epic 7 Story 7.3 `payment-failure-refund-and-support-recovery-paths` 已完成轻量 `bmad-code-review` 并置为 `done`；下一步为 Epic 7 Story 7.4 `launch-readiness-qa-and-regression-safety-net` 生成 story 规格）
- 进入条件是否满足：`是`
- 本阶段产出：Story 7.3 轻量 review 发现并修复 1 个 patch 项：迟到的 `checkout.session.completed` 不再把已退款 purchase 从 `REFUNDED` 改回 `PAID`，也不会重新激活已 `EXPIRED` 的 program；保持 pending/failed/refunded 支付恢复路径、退款撤权、支持文案与隐私边界不变。Review patch 已通过 `pnpm typecheck` 与 `pnpm lint`；本机 focused Playwright 复跑被 Node 18.20.8 阻塞（Next.js 16.1.1 dev server 要求 Node >=20.9.0），不是应用断言失败。
- 下一阶段入口：`bmad-create-story` 生成 Epic 7 Story 7.4 `launch-readiness-qa-and-regression-safety-net`
- 是否允许继续推进：`是`
- 是否需要回退：`否`
- 建议回退到哪个阶段：`无`

## 本轮最小恢复结论

- 本轮优化 Cursor/Codex 双入口规则：`~/.cursor/skills/ask-MyStartupProject1/SKILL.md` 与 `~/.codex/skills/MyStartupProject1/SKILL.md` 已统一为 Win/Mac 双平台路径表达，并新增“当前 chat 上下文加载门禁”：执行其他 skill、BMAD 工作流、story 任务、代码修改或审查前，必须先判断当前 chat 是否已实际读取对应 `SKILL.md`、附属 workflow/checklist/reference/template、目标 story、项目文档与必要代码；缺失或无法确认时先补读再执行。
- Story 7.3 已完成轻量 `bmad-code-review` 并置为 `done`：review 修复 1 个 patch 项，防止 `charge.refunded` 后迟到的 `checkout.session.completed` 重新把 purchase 改回 `PAID` 或重新激活 program；补充 Stripe webhook 回归断言。Review patch 验证通过 `pnpm typecheck`、`pnpm lint`；focused Playwright 复跑因本机 Node 18.20.8 不满足 Next.js 16.1.1 的 Node >=20.9.0 dev server 要求而未启动。下一步进入 Story 7.4 create-story。
- Story 7.2 已完成轻量 `bmad-code-review` 并置为 `done`：基于 `0f68910^..0f68910` 审查，修复 2 个 patch 项：`src/lib/observability/server.ts` / `client.ts` 改为直接使用 Sentry SDK capture 并保留脱敏 stack；`src/app/api/program/report/route.ts` 避免对正常 `not_completed` 状态发 warning 事件，仅对 `missing_content` 保留监控捕获；`e2e/observability.spec.ts` 补充 stack 脱敏覆盖。验证通过 `pnpm typecheck`、`pnpm lint`、`pnpm test:e2e e2e/observability.spec.ts --project="Desktop Chrome"`、`pnpm test:e2e e2e/program-entry.spec.ts --project="Desktop Chrome" --grep "report download"`。本轮同时补齐 `_bmad/bmm/config.yaml` 与本机 `bmad-code-review/checklist.md`，下一步进入 Story 7.3 create-story。
- 本轮继续补强 Cursor/Codex 规则一致性：Cursor 规则文件（Windows `%USERPROFILE%\.cursor\Cursor AI 规则.md`；macOS `~/.cursor/Cursor AI 规则.md`）已加入 BMAD `.cursor/skills/<bmad-identifier>` 执行前读取门禁与任务结束上下文汇报要求；Codex 全局规则（Windows `%USERPROFILE%\.codex\AGENTS.md`；macOS `~/.codex/AGENTS.md`）已加入“任何任务完成后列出当前 chat 已加载关键文件、本轮新增读取/加载文件”的强制规则；项目入口 skill（Windows `%USERPROFILE%\.cursor\skills\ask-MyStartupProject1\SKILL.md`；macOS `~/.cursor/skills/ask-MyStartupProject1/SKILL.md`）已对齐图 2 的项目 BMAD 执行门禁，明确 `bmad-dev-story` 进入单 story 循环前必须读取子 skill、workflow、checklist。
- Story 7.2 规格阶段记录：规格文件 `stories/7-2-monitoring-and-error-observability.md` 已创建；范围限定为 Sentry/approved monitoring 对 caught checkout、Stripe webhook、Day completion、completion report、AI chat provider/quota/stream/RAG/session failures 的可观测捕获；明确隐私边界禁止捕获 email、医疗档案字段、疼痛/notes、聊天正文/回答/citations、report 内容、raw headers/cookies、Stripe secrets/raw checkout URLs；不新增监控供应商、依赖、数据库日志、dashboard、支付/退款恢复 UX、launch QA runbook 或 analytics vocabulary。
- BMAD 流程对齐记录：已按 Windows 精确路径读取 `%USERPROFILE%\.cursor\skills\bmad-dev-story\SKILL.md`（macOS 对应 `~/.cursor/skills/bmad-dev-story/SKILL.md`）、同目录 `workflow.md` 与 `checklist.md`；已补强 Codex 全局规则（Windows `%USERPROFILE%\.codex\AGENTS.md`；macOS `~/.codex/AGENTS.md`）与项目 Codex 入口 skill（Windows `%USERPROFILE%\.codex\skills\MyStartupProject1\SKILL.md`；macOS `~/.codex/skills/MyStartupProject1/SKILL.md`），要求 Cursor 的 Codex 插件在执行任意 `bmad-*` 专项任务前先读取对应 `.cursor/skills/<bmad-identifier>/SKILL.md` 及其附属 workflow/checklist/reference，再进入实现或状态推进。
- Story 6.3 已完成轻量 code-review 并置为 `done`：`CompletionShareAction` 只分享公共产品根链接和通用非诊断文案，未传 program/profile/report/chat/payment/Day 内容；Web Share 成功路径、Clipboard fallback、失败可手动复制公共 URL 均有聚焦 E2E；未新增 `/api/share`、schema/referral、社交追踪、analytics runtime emission、report sharing 或 report 持久化；review 后复验通过 `pnpm typecheck`、`pnpm lint`、6.3 聚焦 Playwright 4 passed / 4 skipped；Epic 6 已标记 `done`，下一步进入 Epic 7 Story 7.1 create-story
- Story 7.1 已生成并更新为 `ready-for-dev`：规格文件 `stories/7-1-product-analytics-and-funnel-events.md` 已创建；范围限定为 typed analytics event registry + client no-op/provider adapter + Landing/Onboarding/Checkout/Day/Chat/Report/Share 代表性埋点；明确默认 disabled/no-op，不新增依赖、Prisma schema、`/api/analytics`、服务端日志、内部 analytics dashboard、监控/Sentry 改造、退款支持流或 launch QA；隐私边界禁止发送 email/userId/programId/payment/session/bodyPart/subType/pain/notes/chat/report/contentJson/provider/model/quotaKey/raw URL query 等敏感信息
- Story 7.1 已执行到 `code-review`：新增 `src/lib/analytics/events.ts` typed event registry + sanitizer、`src/lib/analytics/client.ts` browser-only no-op/provider adapter、`AnalyticsPageView` / `AnalyticsLink` 小组件；已接入 `landing_view`、`cta_click`、`quiz_start`、`quiz_submit`、`checkout_start`、`paid`、`day_completed`、`chat_sent`、`chat_escalated`、`completion_report_view`、`share_click`；默认仅 `NEXT_PUBLIC_ANALYTICS_PROVIDER=plausible|umami` 时启用，未新增依赖、schema、`/api/analytics`、服务端日志或 dashboard；聚焦 E2E `e2e/analytics-events.spec.ts` 通过，验证 `pnpm typecheck`、`pnpm lint`、`pnpm test:e2e e2e/analytics-events.spec.ts`（6 passed / 2 skipped）
- Story 7.1 已完成轻量 `bmad-code-review` 并置为 `done`：发现并修复 1 个 patch 项，`window.__analyticsProvider` 测试覆盖入口现在仅在非生产环境生效，生产环境仍只能通过 `NEXT_PUBLIC_ANALYTICS_PROVIDER=plausible|umami` 启用 analytics；复验通过 `pnpm typecheck`、`pnpm lint`、`pnpm test:e2e e2e/analytics-events.spec.ts`（6 passed / 2 skipped）；`stories/sprint-status.yaml` 已将 `7-1-product-analytics-and-funnel-events` 标记为 `done`，下一步进入 Story 7.2 create-story。
- Story 4.2 已生成并更新为 `ready-for-dev`：规格文件 `stories/4-2-exercise-cards-and-completion-state.md` 已创建；范围限定为从 current `ProgramDay.contentJson.exercises` 渲染 exercise cards，并添加页面内 per-card local completion state 与即时完成百分比；明确不做 server-side day completion mutation、`POST /api/program/day/[day]/complete`、`Program.currentDay` advancement、sticky complete CTA behavior、Chat、analytics、PDF/reporting、billing/refund、i18n、native app 或新依赖
- Story 4.1 已收尾为 `done`：轻量 review 修复两个 patch 项：`contentJson` 标量字符串和字符串数组会 trim，空白 `title/focus` 继续触发 `missing_day_content`；移动端 header metadata 卡片恢复为窄屏纵向堆叠、`sm` 起三列；验证通过 `pnpm typecheck`、`pnpm lint`、`pnpm test:e2e e2e/program-entry.spec.ts --project="Desktop Chrome"`（12/12）
- Story 3.4 已收尾为 `done`：轻量 review 发现并修复两个 patch 项：active paid program 缺 current `ProgramDay` 时返回 `missing_day_content` 而非误报 `no_purchase`；paid purchase 已有关联 inactive program 时 recovery 会恢复 `Program.status = ACTIVE`；验证通过 `pnpm typecheck`、`pnpm lint`、`pnpm test:e2e e2e/program-entry.spec.ts --project="Desktop Chrome"`（10/10）、`pnpm test:e2e e2e/stripe-webhook.spec.ts --project="Desktop Chrome"`（7/7）、`pnpm test:e2e e2e/auth-shell.spec.ts --project="Desktop Chrome"`（15/15）；Epic 3 已置为 `done`，Epic 4 已置为 `in-progress`
- Story 2.3 已收尾为 `done`：轻量 review 无阻塞 patch 项，`stories/sprint-status.yaml` 已更新
- Story 3.3 已收尾为 `done`：轻量 code-review 发现并修复 ProgramDay exactly-14/幂等写入、安全升级语言、exercise bodyPart 校验、missing FAQ 测试覆盖等 patch 项；验证通过 `pnpm typecheck`、`pnpm lint`、`pnpm test:e2e e2e/stripe-webhook.spec.ts`（7 passed / mobile skipped）、`pnpm test:e2e e2e/auth-shell.spec.ts`（30 passed）
- Story 2.4 已执行完成并更新为 `done`：新增 Step 3 Personalized Summary、`POST /api/checkout`、Stripe Checkout service、dev-mock fallback、`/onboarding/checkout/{success,cancelled}`、`STRIPE_SECRET_KEY` env 说明、E2E 覆盖；轻量 code-review 的 3 个 patch 项已修复；未实现 webhook、Purchase、Program、ProgramDay、退款流、billing portal、AI、RAG、analytics、i18n 或原生端
- Story 2.4 已验证：`pnpm db:generate`、`pnpm typecheck`、`pnpm lint`、focused `pnpm test:e2e e2e/auth-shell.spec.ts` 30/30 passed、full `pnpm test:e2e` 46/46 passed；review patch 后再次通过 `pnpm typecheck`、`pnpm lint`、focused `pnpm test:e2e e2e/auth-shell.spec.ts` 30/30 passed
- 本轮稳定化：Recovery Profile submit 补上 hydration guard，避免 React handler 未接管前触发原生表单提交；Playwright 本地 workers 收紧为 1，与 CI 一致，避免 Desktop/Mobile 并发写 auth/profile 状态导致 flake
- Story 2.4 `Personalized Summary and One-Time Checkout` 规格已按真实代码基线生成：基于当前 `src/components/onboarding/eligibility-gate.tsx` 的 `"eligibility" | "profile"` 状态机扩展为 3 状态；用 `onProfileSaved` 回调替换 `recovery-profile-form.tsx` 内部的 "Recovery Profile saved" 占位；新增 `src/components/onboarding/personalized-summary.tsx`、`src/lib/billing/purchase-service.ts`、`POST /api/checkout`、`/onboarding/checkout/{success,cancelled}` 页面；新增依赖 `stripe` 与 env `STRIPE_SECRET_KEY`
- Story 2.4 明确边界：不写 `Purchase` / `Program` / `ProgramDay`、不实现 webhook、不改 `hasPurchase`（全部归 Story 3.2）；无 AI / RAG / 退款流 / 订阅 / i18n / 原生
- Story 2.4 dev-mock 约定：当 `STRIPE_SECRET_KEY` 不存在时，`POST /api/checkout` 返回 `/onboarding/checkout/success?session_id=dev_mock`，保证 CI 与本地 Playwright 无需真 Stripe 即可跑通 CTA→redirect→success 全链路
- `stories/sprint-status.yaml`：`2-3-recovery-profile-multi-step-form: done`、`2-4-personalized-summary-one-time-checkout: done`、`epic-2: done`
- Epic 2 retrospective 已完成并落盘：`stories/epic-2-retro-2026-04-29.md`；核心结论为继续坚持 story 边界，进入 Epic 3 前先冻结 Prisma 数据模型、content 目录结构、pgvector 策略与 purchase-to-program 链接不变量
- Story 3.1 已完成并更新为 `done`：新增/冻结 `Purchase`、`Program`、`ProgramDay`、`ChatConversation`、`ChatMessage`、`KnowledgeDocument`、`KnowledgeChunk`、`content/programs`、`content/faq`、`content/blog`、`content/exercises`；`KnowledgeChunk.embedding` 使用可选 `Unsupported("vector(1536)")?` 并记录 pgvector extension/raw SQL 边界；review patch 已补强 Program/Chat 所有权复合约束、级联删除语义、KnowledgeDocument 唯一键、过期文档表述；明确未做 webhook、program generation、Day UI、AI/RAG runtime、session purchase 状态接线
- Story 3.2 已完成并更新为 `done`：实现并验证 webhook signature/raw body、StripeWebhookEvent durable idempotency、Purchase 写入、Program/14 ProgramDay 初始化、dev-mock success 解锁态与真实 session purchase 字段；轻量 review 修复了并发重复 Stripe webhook 在 Program 唯一约束处返回 500 的幂等风险
- Story 3.3 已实现并更新为 `done`：新增 `content/programs/finger-v1.json`、`content/exercises/finger-basic-v1.json`、`content/faq/finger-basic-v1.json`、`src/lib/program/program-content.ts`；webhook/dev-mock provisioning 现在写入 deterministic `finger-v1` template-first `ProgramDay.contentJson`，校验 `_contract.json` exclusion、exercise/FAQ slug 引用与 exercise bodyPart 一致性，支持幂等升级 Story 3.2 placeholder program、保留 `completionPercent/completedAt` 并清理异常非 1-14 day；明确未做 Day UI、AI/RAG、Chat、analytics、refund UI、billing portal、subscription、i18n 或原生端
- Story 2.2 已执行完成并更新为 `code-review`，`epic-2` 保持 `in-progress`
- Story 2.2 落地范围：基于当前真实 `/onboarding` 登录保护继续迭代，新增 deterministic eligibility classifier、Step 1 Eligibility & Safety Gate UI、eligible / not eligible / clinician-attention 三类结果、hydration-safe submit、auth E2E 覆盖；未引入 RecoveryProfile、Purchase、Program、Stripe、AI、RAG、analytics、i18n 或 Prisma 业务模型
- Story 2.2 已验证：`pnpm typecheck`、`pnpm lint`、`pnpm test:e2e e2e/auth-shell.spec.ts`、`pnpm test:e2e` 全部通过；完整 E2E 为 28 passed
- 本轮为稳定 Windows + Next.js 16 E2E，将 Playwright 本地 webServer 改为 `next dev --webpack`、本地 workers 限制为 4、test timeout 提升到 60s，避免 Turbopack 并发导航 panic
- 本轮已按 `stories/sprint-status.yaml` 自动发现口径生成 Epic 2 Story 2.2：`<项目根目录>/stories/2-2-eligibility-safety-gate.md`
- Story 2.2 已更新为 `ready-for-dev`，`epic-2` 保持 `in-progress`
- Story 2.2 规格基于当前真实 Auth / Onboarding 骨架：继续复用 `/onboarding` 的登录保护、`getAuthSession()`、NextAuth v4 最小 session、dev-only login 与现有 E2E；范围仅做 Eligibility & Safety Gate，不提前引入 RecoveryProfile、Purchase、Program、Stripe、AI、RAG、analytics 或 Prisma 业务模型
- 本轮已将 Story 1.2 标记为 `done`，并按 `stories/sprint-status.yaml` 自动发现口径生成 Epic 1 Story 1.3：`<项目根目录>/stories/1-3-safety-faq-conversion-confidence.md`
- Story 1.3 已执行完成，轻量 code-review patch findings 已修复，并更新为 `done`
- Story 1.3 本轮落地范围：扩展 `landing-safety`，新增 `landing-faq` 可访问 FAQ client component，新增 `landing-footer-cta`，并补强 E2E；未做 Onboarding、Stripe、Eligibility Gate、analytics、SEO blog 或 AI runtime safety
- Story 1.3 已验证：先红灯确认缺少 FAQ/Footer CTA 与 Safety 文案不足，再 `pnpm typecheck`、`pnpm lint`、`pnpm test:e2e` 全部通过（12 passed）；review patch 修复后再次通过 typecheck/lint/e2e（12 passed）
- 本轮已按 `stories/sprint-status.yaml` 自动发现口径生成 Epic 1 Story 1.4：`<项目根目录>/stories/1-4-public-trust-content-seo.md`
- Story 1.4 已执行完成并通过轻量 `bmad-code-review`；唯一 patch finding 已自动修复，Story 1.4 与 Epic 1 均标记为 `done`
- Story 1.4 落地范围：基于当前真实 Landing / legal shell 创建公开 SEO 内容入口 `/blog/finger-stiff-after-cast-removal`、typed blog content source、article metadata、`/legal/disclaimer` trust link、`/onboarding` CTA 与 E2E；未做 CMS、MDX、analytics、Onboarding、Stripe、RAG 或 Prisma schema
- Story 1.4 review 修复：将 blog content interfaces 显式收紧为 readonly，并在传入 Next metadata keywords 时复制为 mutable array，避免内容注册表后续被误改或与 Metadata 类型冲突
- 本轮已按 `stories/sprint-status.yaml` 自动发现口径生成 Epic 2 Story 2.1：`<项目根目录>/stories/2-1-magic-link-identity-session.md`
- Story 2.1 已执行完成并更新为 `code-review`，`epic-2` 保持 `in-progress`
- Story 2.1 落地范围：基于当前真实 Next.js / provider / marketing shell 代码继续迭代，落地 NextAuth v4 Magic Link identity session 的最小可验证骨架；已新增 NextAuth Prisma auth tables、Magic Link route、最小 session fields、dev-only local login、protected onboarding skeleton 与 E2E；未做 Eligibility Gate、Recovery Profile、Stripe、Purchase、Program、RAG、AI 或 analytics
- Story 2.1 已验证：先红灯确认 6 个 auth E2E 失败且 16 个 public shell 用例仍通过；实现后 `pnpm db:generate`、`pnpm typecheck`、`pnpm lint`、`pnpm test:e2e` 全部通过（22 passed）
- 本轮已完成 Story 1.1 验收：`pnpm install` 成功，`pnpm typecheck` 通过，`pnpm lint` 通过，`pnpm test:e2e` 在 Desktop Chrome + Mobile Chrome 共 8 个用例全部通过
- 本轮已生成并执行 Epic 1 Story 1.2，文件：`<项目根目录>/stories/1-2-landing-hero-core-value.md`
- Story 1.2 已从 `ready-for-dev` 经轻量 code-review 自动修复后切换到 `done`
- Story 1.2 本轮落地范围：
  - 更新真实代码 `src/app/(marketing)/page.tsx`：正式 Hero 价值主张突出 `critical 2 weeks after cast removal`
  - 更新 `src/components/marketing/marketing-header.tsx`：Header 主 CTA 固定为 `Start my 2-minute quiz`，指向 `/onboarding`
  - Hero 主 CTA 固定为 `Start my 2-minute quiz`，次 CTA 固定为 `See how the 14-day plan works`
  - 新增 `landing-pain-points` / `landing-what-you-get`，并重写 `landing-how-it-works`
  - 更新 `e2e/marketing-shell.spec.ts` 覆盖 Story 1.2 的 Hero、CTA、核心区块，同时保留 legal / 404 回归
  - 已验证：`pnpm typecheck`、`pnpm lint`、`pnpm test:e2e` 全部通过；轻量 review 自动修复后 E2E 为 10 passed
  - 未实现 FAQ、完整 Safety、Footer CTA、Onboarding、Stripe、埋点或 SEO 内容；其中 FAQ / Safety / Footer CTA 已进入 Story 1.3
- Story 1.1 已从 `code-review` 切换到 `done`
- Story 1.1 本轮落盘的关键文件清单：
  - 工程配置：`package.json` / `tsconfig.json` / `eslint.config.mjs` / `postcss.config.mjs` / `next-env.d.ts` / `.gitignore` / `.env.example`
  - Next.js 基线：`next.config.ts` / `instrumentation.ts` / `sentry.{client,server,edge}.config.ts`
  - 测试：`playwright.config.ts` / `e2e/marketing-shell.spec.ts`
  - 数据：`prisma/schema.prisma`（仅 datasource / generator 占位）
  - 代码：`src/app/layout.tsx` / `src/app/globals.css` / `src/app/not-found.tsx`
  - 代码：`src/app/(marketing)/{layout,page}.tsx` + `src/app/(marketing)/legal/{privacy,terms,refund,disclaimer}/page.tsx`
  - 代码：`src/components/providers/{index,query-provider,session-provider}.tsx`
  - 代码：`src/components/ui/{button,sonner}.tsx` + `src/components/marketing/{marketing-header,marketing-footer}.tsx`
  - 代码：`src/lib/{prisma,query-client,utils}.ts`
  - 脚本：`scripts/verify-env.ts`（裁剪到 Story 1.1 env 集）
  - 文档：`README.md` 补 `Getting Started` 与 `lease-guard` 复用对照
- 与 Story 规格的偏差：`@prisma/client` + `prisma` 已加入 `package.json`（并加了最小 `prisma/schema.prisma`），因为 AC#3 要求 `src/lib/prisma.ts` 必须可通过类型检查；这是对原 story 任务清单的合理增量
- v1 英文单站约束已执行：未引入 `next-intl` / `[locale]` / `dictionary-provider`
- Hero 正式文案 / Pain Points / FAQ / Safety 内容显式推迟到 Story 1.2 / 1.3 / 1.4，未在本轮实现
- 验证备注：Playwright mobile 项目已从 `devices["iPhone 12"]` 修正为 `devices["Pixel 5"] + browserName: "chromium"`，避免误触发 WebKit 下载；Story 1.1 的 mobile 验收目标仍是 Chromium 移动视口 smoke
- 本轮完成 Epic 1 Story 1.1 规格，文件：`<项目根目录>/stories/1-1-marketing-shell.md`
- Story 1.1 核心范围：
  - 从 approved stack（Next.js 16 + React 19 + TS 5 + Tailwind 4 + pnpm）初始化工程
  - 严格按 `技术架构详细设计.md §3.1` 复制 `lease-guard` 基础骨架（next.config / instrumentation / sentry / playwright / prisma / query-client / providers / verify-env）
  - 落地 `(marketing)` 路由分组、root layout、共享 `MarketingHeader` / `MarketingFooter` / 4 个 legal 占位页
  - 明确 v1 英文单站，不引入 `next-intl` / `[locale]` / `dictionary-provider`
  - Hero 文案 / Pain Points / FAQ / Safety 内容区块显式推迟到 Story 1.2 / 1.3 / 1.4
  - 附带 Playwright shell smoke E2E（mobile + desktop）
- 当前 `stories/sprint-status.yaml` 仍是 story 自动发现入口；`1-1-marketing-shell` 为 `done`，`1-2-landing-hero-core-value` 为 `ready-for-dev`，`1-3-safety-faq-conversion-confidence` 是下一条 backlog
- 当前建议顺序更新为：先执行 Story 1.2，再基于真实代码回来生成 Story 1.3
- 本轮完成 `epics.md`，文件：`<项目根目录>/epics.md`
- Epic 结构已收敛为 7 个用户价值导向 epic：
  - `Epic 1` Discover and Trust the Recovery Coach
  - `Epic 2` Become an Eligible Paid User
  - `Epic 3` Receive a Personalized 14-Day Program
  - `Epic 4` Complete Daily Recovery Actions with Confidence
  - `Epic 5` Ask Safe Recovery Questions with AI
  - `Epic 6` Finish the Program and Share Outcomes
  - `Epic 7` Operate, Measure, and Launch Reliably
- stories 已全部写入 `epics.md`，并覆盖：
  - `FR1-FR14`
  - 关键 NFR
  - 架构附加要求
  - `UX-DR1-UX-DR12`
- 本轮完成 `README.md`，作为项目入口文档，已补齐：
  - 项目简介
  - 当前状态
  - 核心文档导航
  - 建议阅读顺序
  - 当前主线约束
  - 下一步入口
- 当前建议顺序更新为：`bmad-create-story`（为 Epic 1 / Story 1.1 开始生成单 story 规格）或直接 `bmad-agent-dev` 进入实现
- 本轮完成 `UX设计规格说明.md`，文件：`<项目根目录>/UX设计规格说明.md`
- UX 主结论：
  - `Landing` 已冻结为 7 个区块：Hero / Pain Points / How it Works / What You Get / Safety / FAQ / Footer CTA
  - `Onboarding` 已冻结为 3 步：Eligibility Gate / Recovery Profile / Personalized Summary + Checkout CTA
  - `Day` 已冻结为 6 个区块：Recovery Header / Today’s Focus / Exercise Cards / Normal vs Get Help / Quick Questions / Sticky Complete
  - `Chat` 已冻结为 4 个区块：Context Header / Suggested Prompts / Chat Stream / Input Area
  - 每个页面都已补：入口出口、关键交互、状态矩阵、埋点、story 拆分建议
- readiness 状态更新：`UX 缺口已补齐到可拆 story 粒度`
- 当前建议顺序更新为：`直接进入 bmad-create-epics-and-stories`
- 本轮完成 `implementation readiness` 首轮检查，结论：`Not Ready`
- 通过项：
  - `Product Brief` 已完成
  - `路线图与 MVP` 已完成
  - `技术架构 detailed design` 已完成
- 缺口项：
  - `UX` 仍未达到可拆 stories 的稳定度；虽然已有信息架构，但还缺关键页面的页面级结构冻结
  - `epics/stories` 仍未创建，因此当前不能判定为真正 implementation ready
  - `content/` 结构化方案（尤其 `guzhe` 案例、动作资产、FAQ、program templates）仍未冻结为实施输入
  - `Program / ProgramDay / KnowledgeChunk` 的最终字段还未明确锁版
- 本轮已在 `技术架构详细设计.md` 中补充 `Landing / Onboarding / Day / Chat` 的最小 UX 结构草案，作为下一轮拆 stories 的前置输入
- 当前建议顺序：`先补轻量 UX 收口` -> `再拆 epics/stories` -> `再做第二轮 readiness 复检`
- 本轮完成 `技术架构详细设计.md`，文件：`<项目根目录>/技术架构详细设计.md`
- 架构主结论：
  - 基础设施继续沿用 `D:\work\MyStartupProject\lease-guard`，但仅复用 `Next.js / Prisma / Provider / Stripe 封装模式 / Sentry / Playwright / verify-env` 等基础层
  - 首版明确不继承 `lease-guard` 的 `next-intl + [locale]` 结构，改为 `英文单站`
  - 支付正式收敛为 `Stripe 一次性 Checkout`，不复用订阅、账单门户、套餐升级降级逻辑
  - Program 生成策略收敛为 `模板优先 + 规则映射 + LLM 微调`，不让模型直接生成完整康复方案
  - AI Chat 收敛为 `RAG + 危险信号护栏 + 每用户每日 20 次 Upstash 限流`
  - 数据层建议 `默认开发也使用 Supabase Postgres`；SQLite 仅保留给纯 UI 离线开发，避免 `pgvector` 与生产 schema 漂移
- 当前技术方案已足以进入 `implementation readiness` 检查，但大概率仍需补 `UX 结构收口` 与 `stories 拆解`
- 本轮完成 `Product Brief v1.1`，文件：`<项目根目录>/产品Brief.md`
- 5 项关键决策（极窄极深 + 0 投资 + 纯口碑）：
  - 付费模式：`一次性 $14.99 买断单档位`（v1.1 从 $19.99 微调，感知降价保留工具档位；放弃月订阅 / 双轨并行 / $9.99 便宜 App 档）
  - 首版部位：`仅手指 / 掌骨 1 个部位`（放弃首版 3 部位全覆盖，聚焦 guzhe 案例深做）
  - 前端技术栈：`Next.js 16 App Router + React 19 + Vercel`，基础栈沿用 `D:\work\MyStartupProject\lease-guard`（pnpm + Tailwind 4 + shadcn/ui + Prisma 6 + NextAuth.js v4 + Stripe + Vercel AI SDK + Upstash Redis + Sentry + Jest + Playwright；18 项配置资产直接复制，预计节省 ~30h）
  - 动获渠道：`SEO 长尾 + Reddit 垂直板块`（r/fractures 等）
  - 推荐裂变：`纯口碑 + 分享按钮`（首版不做激励机制）
- KPI 按 $14.99 一次性买断口径重算（原 MRR / 30 日留存不适用）：
  - 3 月：`60-100 付费 / $900-1500 累计营收 / 转化 ≥ 2% / 14 天完成率 ≥ 40% / ≥ 5 有机 referral`
  - 6 月：`累计 300-500 付费 / $4500-7500 营收 / 扩展足踝`
  - 12 月：`累计 1000-2000 付费 / $15000-30000 营收 / 扩展锁骨 + 中国站启动`
- North Star Metric：`14 天完整完成率 ≥ 40%`（低于 30% 则回退 `问题 / 需求验证`）
- 历史结论（沿用）：Conditional Go + 首版只做海外（美国英语站）+ A 型主画像 + 6 项风险清单
- 用户约束：`3 月时间盒 / 个人无公司 / 0 投资 / 免费云 + 免费 API / 兼职 / 无其他资金`
- 地域决断：**首版只做海外（美国英语站）**，中国侧延后到有营业执照再进入；决断依据：
  - 中国：支付 / ICP 备案 / 小程序医疗类目均需主体，0 投资无公司全部阻塞
  - 海外：Stripe / LemonSqueezy / Paddle 支持个人主体；Vercel + Cloudflare + Gemini 免费 Tier 可当天上线；FDA SaMD 可用 `教育 / 陪伴` 定位回避
- KPI 调整建议（诚实重定标，避免 3 月硬追 $6k MRR 的不现实目标）：
  - 原目标：3 月 500 付费 / 月流水 5000 元 ≈ 海外 $700 MRR，但 500 × $12.99 = $6495 MRR，属独立开发者前 5% 爆发案例
  - 推荐 3 月目标（PMF 信号）：`30-100 付费 / $300-1000 MRR / 付费转化 ≥ 2% / 30 日留存 ≥ 40% / ≥ 5 个有机 referral`
  - 推荐 6 月目标：`300-500 付费 / $2000-5000 MRR`
  - 推荐 12 月目标：`1000-2000 付费 / $8000-15000 MRR`
- 风险清单：
  - SEO 冷启动 3-6 月见效，3 月内达标挑战 → 并行 Reddit / Twitter / FB 群 / ProductHunt
  - FDA SaMD 合规边界 → `education & companion, not medical device` 定位 + 免责声明
  - 免费 API 额度 → 每用户日限额 + Gemini Flash 替 Pro
  - 用户生命周期 3-6 月 → 转介绍激励 + 部位扩展（手指 → 足踝 → 锁骨）
  - 无医师背书 → guzhe 真实日记 + AAOS / NHS 公开协议做内容权威
  - 英文非母语 → AI 辅助写作 + 美国康复师 KOL 访谈
- 继续 Go 的 5 个依据：切口独特（骨折细分空白）+ 付费触发硬（拆石膏窗口）+ 独开经济账可算（LTV $30-80 vs CPA $5-10）+ 有真实案例（guzhe + 本人亲历）+ 0 投资技术栈完全可行
- 下一步默认：用户确认调整后 KPI → `bmad-product-brief` 开始 Product Brief
- 目标用户（A 型主画像，首版唯一聚焦）：
  - `拆石膏前后 2 周的手部 / 足踝 / 锁骨骨折患者`，25-45 岁为主，大学以上 / 中国月薪 8000+ / 海外年薪 $50k+
  - 情绪：`怕 + 急 + 孤独 + 无力`；黏连窗口不可逆是结构性焦虑来源
  - 行为：搜索引擎长尾 + 小红书 / 抖音 + 好大夫图文问诊但 "答案不成系统"
  - 预算：中国 99-199 元 / 海外 $19.99-39.99；决策时长 < 30 分钟（冲动决策型）
- 目标用户明确不打：`老年髋骨 / 复杂骨折（粉碎、开放、多发）/ 儿童 / 怀疑骨折未就医`
- 次画像 B 型（后期扩展）：`18-35 岁运动伤骨折`，付费力更强，愿为 1v1 专家视频 $49-59 买单
- 核心价值主张一句话（中文 / 英文双版，可直接作为 landing page 主标题）：
  - 中：`骨折拆石膏后 2 周，按你的部位和阶段每天教你怎么动，24 小时 AI 答疑，不用再排队 3 分钟复查`
  - 英：`Your 24/7 fracture recovery coach — personalized daily exercises for the critical 2 weeks after cast removal`
- 差异化矩阵（与 6 类替代方案均有清晰边界）：
  - `好大夫 / 丁香医生`：他们图文问诊 5 分钟一答；我方做结构化 2 周计划 + 24h 陪伴 + 动作计时器
  - `骨事通 / 骨典 / 复骨医疗`：他们绑硬件 + 医院分诊；我方独立 C 端直接付费 + SEO 长尾获客 + AI 答疑
  - `Curovate`：他们只做 ACL / 膝 / 髋；我方专攻骨折（手部 / 足踝 / 锁骨 Curovate 完全不做）
  - `小红书 / 抖音 KOL`：碎片科普 + 卖课；我方按阶段个性化 + 即时答疑
  - `Hinge / Sword / Kaia`：完全走 B2B2C 雇主保险，不触达 C 端独立付费
  - `Google / 百度自搜`：碎片信息；我方权威整合 + 可执行计划 + 焦虑缓解
- 购买触发点（5 个硬触发点，全部满足 `时间敏感 + 替代稀缺 + 30 分钟决策 + 99-199 元客单`）：
  - `拆石膏当天医生"回家自己多动"之后`（最强，30 分钟内）
  - `拆石膏前 2-3 天，复查预约后预习`（强，1-2 天）
  - `拆石膏后 3-5 天，关节僵硬恐慌期`（强，即时）
  - `复查前 1 周，二次焦虑峰值`（中，3-7 天）
  - `病友社群推荐`（长尾，客单更高）
- 伪触发点（避免误判）：`术后 1 周太早` / `康复 3 月后焦虑已缓解` / `纯信息需求无行动压力` / `怀疑骨折未就医属合规红线`

## 项目背景与长期目标

- 用户为 `中国大陆、无公司` 的独立开发者，前端经验强，覆盖 `React`、`React Native`、`Vue 2/3`、`TypeScript`、`JavaScript`、`小程序`、`Flutter`、`iOS`、`Android`
- 目标是做一个既能赚 `人民币` 也能赚 `美元` 的产品，前期投入低、开发周期短、需求面大、留存扎实、付费转化健康，并具备一定被动收入潜力
- 历史背景：之前做过 `D:/work/MyStartupProject`，上线后用户不多；因此这一轮优先级是 `真实需求验证 > 市场空间 > 付费可能性`
- 当前默认约束：先判断 `做不做`，再判断 `怎么做`；未完成上游验证前，不进入 `Product Brief / MVP / 架构 / 开发`

## 当前主线判断

- 当前首推：`骨折康复 AI 陪伴（C 端，窄切：拆石膏前后 2 周，首版只做海外）`
- 状态：`问题 / 需求验证 通过` + `市场与竞品 通过` + `问题定义与价值主张 通过` + `立项决策 Conditional Go`，已可推进到 `Product Brief`（待用户确认调整后 KPI）
- 首版地域：`海外美国英语站` 唯一；`中国` 等营业执照后二期再入（约 6-12 月）
- 首版 3 月目标（PMF 信号）：`30-100 付费 / $300-1000 MRR / 付费转化 ≥ 2% / 30 日留存 ≥ 40% / ≥ 5 有机 referral`
- 技术栈 0 投资方案：`Vercel / Cloudflare Pages 免费 Tier + Gemini 免费 Tier + Stripe / LemonSqueezy`
- 第二候选（保底）：`ACL 术后康复`，综合 67/80，若主线验证遇到硬反例可回退
- 不推荐为主线：`运动损伤（伞）` / `慢性腰颈肩`，原因均为正面撞 `Hinge Health / Sword Health / Kaia Health`；Sword 2026-01 已整合 Kaia，B 端更集中
- 已撤回：`英语口语 AI 陪练`，原因 `赛道过度拥挤 + 独开获客难 + 成本紧张`

## 当前主线固定模板

- 主线名：`骨折康复 AI 陪伴（C 端，窄切：拆石膏前后 2 周）`
- 推荐理由：`竞品最弱 + 规模足够（全球每年骨折约 1.7 亿例，中国每年 1000-2000 万新增）`
- 推荐理由：`立即付费触发最强（拆石膏前后 2 周焦虑峰值，公立医院复查只 3 分钟）`
- 推荐理由：`有真实案例作为差异化根基（guzhe 手指骨折治疗计划 + 用户亲历经验）`
- 推荐理由：`合规风险可控（有成熟骨科康复规范，不涉及诊断，比慢病 / 心理 / 肿瘤安全）`
- 推荐理由：`独开可做（AI 生成个性化计划成本低，SEO 长尾流量红利仍在）`
- 推荐理由：`基础栈可沿用已上线项目 lease-guard（Next.js 16 + 完整 SaaS 套件），首周技术初始化提速 50%，累计节省 ~30h`
- 首版功能：`按部位 + 阶段的康复计划（拆石膏后优先，手指 → 足踝 → 全身扩展）`
- 首版功能：`每日动作指导 + 视频 + 计时器 + 打卡记录`
- 首版功能：`AI 答疑（常见焦虑：还肿 / 僵硬 / 多久能 xx）`
- 首版功能：`一次性 $14.99 购买解锁 14-day personalized recovery plan`
- 不做什么：`不做诊断 / 不替代医生 / 不给治疗方案`
- 不做什么：`不做机构侧（康复师 / 诊所管理），B2B 冲突保持排除`
- 不做什么：`不首版做运动损伤全覆盖，避免撞 Hinge Health`
- 不做什么：`不首版做慢性腰颈肩，避免撞成熟红海`
- 下一步验证：`典型用户旅程 + 真实付费触发点（拆石膏前后 2 周焦虑是否足够具体）`
- 下一步验证：`SEO 长尾关键词流量规模（手指骨折拆石膏 / 脚踝骨折多久能走 / 锁骨骨折康复 等）`
- 下一步验证：`中国大陆 + 海外竞品分析（国内 骨气 / 康复派 / 好大夫 / 壹健康；海外 Curovate / Sera）`
- 下一步验证：`首发终端选择：网站（SEO 长尾）vs App（计时器 + 推送打卡）`

## 首发终端建议

- 首版明确：`英文网站 + 海外支付（Stripe / LemonSqueezy）` 先上线，美国英语用户为唯一早期用户群体
- 第二步再评估 `轻量 PWA / App（iOS + Android）` 作留存抓手（计时器 / 推送 / 打卡）
- `小程序 + 中国站` 推迟到有营业执照再做，不进入首版范围
- 终端顺序固定：`英文网站 → 轻量 App / PWA → 中国站（待主体）`

## 微信小游戏 / H5 小游戏判断

- `微信小游戏`：如果目标是 `人民币收入`、`快速测试泛娱乐玩法`、`依赖微信生态分发`，它比 `H5 小游戏` 更现实
- `微信小游戏`：2026 年平台扶持和变现政策明显更积极，`IAA` 与 `IAP` 激励都更强
- `微信小游戏`：但它的核心问题是 `强平台依赖`、`更像 hit-driven 生意`，而且 `个人主体` 上架虽可行，但 `支付 / 内购` 能力受限，想做强变现通常仍更适合企业/个体主体
- `微信小游戏`：对你想同时拿 `美元 + 人民币` 这件事帮助有限，更像偏 `中国平台内的人民币项目`
- `H5 小游戏`：优点是 `全球可访问`、技术栈更贴近你现有前端能力、理论上更容易赚美元
- `H5 小游戏`：但核心难点是 `分发比微信小游戏更难`、独立开发者广告收入不稳定、没有强平台流量时很容易做了却没人玩
- `综合比较`：如果你就是想做 `泛娱乐 / 轻游戏`，`微信小游戏` 明显优于 `H5 小游戏`
- `综合比较`：如果你想做的是 `低平台依赖、可持续复利、同时兼顾人民币和美元` 的长期总主线，`微信小游戏` 和 `H5 小游戏` 目前都不够理想
- `当前判断`：`微信小游戏 / H5 小游戏` 可以作为重新打开的候选分支，但暂不建议直接升为当前整体第一优先总主线

## 已排除或降级方向

- `本地服务商家预约爽约挽回助手`：`放弃为当前主线`，原因是 `核心付费者是商家/企业`，与当前 `优先 B2C` 约束冲突
- `本地服务商家预约爽约挽回助手`：保留含义是 `需求真实但用户类型不匹配`，除非后续重新开放 B2B，否则不再作为当前第一优先
- `微信小游戏 / H5 小游戏`：`当前不升为第一优先总主线`，原因是 `强平台/流量依赖 + hit-driven + 美元与人民币双端兼顾较弱`
- `微信小游戏 / H5 小游戏`：当前保留含义是 `如果你明确想做泛娱乐或先跑中国平台内广告/IAP模型，可以单独开一条候选支线`
- `ACL 术后康复支持`：`降级为高优先备查方向`，原因是 `可以继续验证，但更容易落到小程序 / App / 医疗相关约束，不天然适合当前默认首推的网站首发`
- `ACL 术后康复支持`：当前保留含义是 `可继续做候选研究`，但 `不是整个项目当前第一优先主线`
- `求职冲刺助手`：`降级为备选`，原因是 `赛道拥挤 + 平台内置能力强 + 泛化切口缺少差异化`
- `苹果设备维修决策助手`：`排除为当前主线`，原因是 `低频 + 弱复购 + 收入上限偏低`
- `推荐靠谱第三方维修店`：`排除为主收入模型`，原因是 `更适合辅助能力 + 商家付费不稳定 + 平台与合规风险更高`
- `机构侧康复管理工具`：`当前排除`，原因是 `需要跑渠道 / 谈合作 / 启动更重`
- `AI 互动剧情 / 角色扮演产品`：`已降级`，原因是 `免费 AI` 难以支撑早期付费价值`
- `情感关系恢复与沟通助手`：`已从第一优先降级为待验证候选`，原因是 `用户在 分手 / 冷战 当下未必会主动打开独立网站求助`
- `情感关系恢复与沟通助手`：当前保留含义是 `痛点可能真实，但产品入口与形态未通过验证`
- `AI 陪伴 / 互动剧情`：`本轮定为备选`，原因 `赢家通吃 + 内容 / 社区重运营 + 免费替代强`，单人开发早期突围难
- `健康改善 / 减脂睡眠`：`本轮定为备选`，原因 `立即付费触发弱 + 行为改变难 + 订阅流失高`
- `学术英语 / 论文润色`：`本轮定为备选（主线的细分衍生）`，原因 `用户规模相对窄 + 场景低频`，更适合作为英语学习主线下的延伸切口
- `英语口语 AI 陪练（职场 / 面试切口）`：`从第一主线撤回为备选`，原因 `赛道过度拥挤（Speak / Cambly / 流利说 / 多邻国 / 豆包 自带口语） + 独开 SEO 与付费获客难 + LLM+TTS 成本紧张`
- `个人康复 AI 陪伴（C 端）`：`重新打开为候选池`，原降级理由 `更适合 App/小程序 > 网站` 被判定为不够硬（终端选择不应倒推方向否决）；`医疗合规约束` 可通过 `非医疗陪伴` 定位管理
- `运动损伤（伞）`：`本轮不推荐为主线`，原因 `正面撞 Hinge Health / Sword Health / Kaia Health`，独开难
- `慢性腰颈肩`：`本轮不推荐为主线`，原因 `正面撞 Hinge Health / 赛道极卷 / 立即付费触发弱`
- `ACL 术后康复`：`本轮保留为第二候选`，原因 `综合 67/80，垂直清晰但规模略窄于骨折康复`
- `机构侧康复管理工具`：`继续保持排除`（B2B 与当前优先 B2C 约束硬冲突）
- 历史结论订正：`骨气 App 已停更` 无法证实，实际国内活跃的是 `骨事通 / 骨典 / 复骨医疗`，均为 B2B2C 模式；不作为主线排除依据，但作为竞品事实记录
- `独立开发者出海发布工具箱`：`暂不建议作为当前主线`，原因是 `目标用户偏窄 + 单点功能付费意愿偏弱`
- `跨境电商 / 独立站广告拒审与合规修复助手`：`当前不首推`，原因是 `专业门槛更重 + 早期更吃信任`
- `中小商家催款与账期风险助手`：`当前不首推`，原因是 `更偏 B2B + 更像销售推进`
- `网站隐私政策 / Cookie / 合规文档自动维护助手`：`本轮排除为当前主线`，原因是 `痛点不够强即时 + 成熟竞品较多 + 容易被拖延支付`
- `AI 客服知识库 / 重复问答拦截助手`：`本轮排除为当前主线`，原因是 `市场拥挤 + 更偏效率优化而非立即止血`
- `中小企业应收账款催收自动化助手`：`本轮排除为当前主线`，原因是 `偏 B2B 流程接入 + 销售推进成本更高`

## 最新待继续问题

- **Story 3.1 已完成并标记 `done`**；轻量 `bmad-code-review` 的 5 个 patch 项已修复，4 个 defer 项已记录到 `stories/deferred-work.md`
- Story 3.1 当前验证结果：review patch 后 `pnpm db:generate`、`pnpm typecheck`、`pnpm lint`、focused `pnpm test:e2e e2e/auth-shell.spec.ts` 全部通过；focused E2E 30/30 passed
- Story 3.1 关键边界仍成立：只冻结 Prisma domain models、pgvector raw-SQL 边界与 `content/` 输入契约，不做 webhook unlock、program generation、Day UI、AI/RAG runtime 或 session purchase 状态接线
- **epics/stories 已完成并落盘**；当前 epic-3 的 Story 3.2 已 ready-for-dev
- 路线图核心结构（13 周 / 260 小时 / 6 Gate）：
  - Phase 0 Foundation (W1-2) → M1 技术骨架 + 法律文档
  - Phase 1 Content MVP (W3-4) → M2 14 天课程 + 10 视频 + RAG 入库
  - Phase 2 Product MVP (W5-6) → M3 端到端漏斗 + Stripe 真收
  - Phase 3 Polish + Prep (W7-8) → M4 SEO 种子 5 篇 + Reddit 养号
  - Phase 4 Soft Launch (W9-10) → M5 Reddit 首发 + 前 10-20 付费
  - Phase 5 Scale + PMF (W11-13) → M6 ProductHunt + PMF 验收
- G6 PMF 分级决策矩阵（W13 末）：
  - ≥60 付费 + ≥40% 完成率 → 启动 v1.1 足踝扩展
  - 30-60 付费 + ≥30% 完成率 → 延期 4-8 周继续优化单部位
  - 30-60 付费 + <30% 完成率 → 回退 `路线图与 MVP`（产品 / 内容重设计）
  - <30 付费 → 回退 `问题 / 需求验证`（痛点 / 渠道 / 定价判断错）
- `bmad-check-implementation-readiness` 首轮检查结果：
  - `Brief / 路线图 / 技术方案` 已具备
  - `UX` 已补齐到可拆 story 粒度
  - `epics/stories` 已具备
  - 因此当前顺序已更新为“进入单 story 规格或直接实现”
- 下一轮 UX 收口需锁定的 4 个页面：
  - `Landing`：Hero / 痛点 / How it works / What you get / FAQ / CTA
  - `Onboarding`：Eligibility Gate / Recovery Profile / Summary + Checkout CTA
  - `Day View`：顶部进度 / Today Focus / Exercise Cards / Normal vs Get Help / Sticky Complete
  - `Chat`：Context Header / Suggested Prompts / Chat Stream / Danger Escalation / Quota State
- UX 收口后需立即冻结的实施输入：
  - `Program / ProgramDay / KnowledgeChunk` 最终字段
  - `content/` 结构（program templates / FAQ / blog / exercise assets）
  - stories 拆分维度：`Foundation / Billing / Program / Chat / Content / Analytics`
- 当前活跃并行 spike（不阻塞 `bmad-agent-architect`）：
  - 🔍 SEO 关键词实测（W1-2）
  - 🏷️ 商标 / 域名清查（W1-2）
  - 📝 guzhe 案例英文化改写（W2-4）
  - 🤝 康复师 KOL 接触（W3-6）
- 锁定规则：任何关键决策变更（付费模式 / 部位范围 / 技术栈 / 渠道 / 裂变机制）需回 `立项决策` 重评并同步主文档
- 流程纪律：每轮任务完成后必须立即把结论最精简地回写本文件
- 每轮回答默认只更新本文件；除非 `workflow / 路由 / 模板` 发生变化，否则不修改 SKILL.md

## 文档维护规则

- `<项目根目录>/项目主档案.md` 是当前项目事实的唯一主文档
- `<项目根目录>/.cursor/rules/project-context.mdc` 当前不存在；在补建前，继续以本文件作为新 chat 恢复上下文的唯一来源
- 这里统一维护：`当前阶段`、`候选方向`、`竞品结论`、`排除理由`、`下一步`
- `SKILL.md` 只维护：`工作流`、`路由规则`、`输出模板`、`边界`

## 下一步

- 当前建议下一步：
  1. 对 **Story 7.4**（`7-4-launch-readiness-qa-and-regression-safety-net`，当前 `backlog`）执行 `bmad-create-story`。
  2. 进入前先按当前系统读取 `bmad-create-story` skill：Windows `%USERPROFILE%\.cursor\skills\bmad-create-story\SKILL.md`；macOS `~/.cursor/skills/bmad-create-story/SKILL.md`，以及其附属 `workflow.md` / `checklist.md` / `template.md`；再读取 `stories/sprint-status.yaml`、`epics.md`、Story 7.3 真实落地文件与本轮 review 结论，把 Story 7.3 的实际代码现状写入 Story 7.4 Dev Notes。
- 目标阶段：`功能开发`（Epic 7 in-progress；Story 7.1 done；Story 7.2 done；Story 7.3 done；Story 7.4 backlog）
- 进入条件是否满足：`满足`，Story 7.3 已完成轻量 `bmad-code-review` 并在 `stories/sprint-status.yaml` 标记为 `done`；当前 epic 的下一条 backlog 是 Story 7.4。
- 本轮最小恢复结论：`Story 7.3 review 已完成：修复迟到 checkout completed 事件可能覆盖 refunded 状态并重新激活 program 的问题；补充 Stripe webhook 回归断言；review patch 已通过 typecheck/lint。Focused Playwright 复跑未启动，原因是本机 Node 18.20.8 不满足 Next.js 16.1.1 dev server 的 Node >=20.9.0 要求。下一步进入 bmad-create-story 生成 Story 7.4 launch readiness QA/regression safety net。`
- 若后续实现中出现冲突：`优先以 stories/*.md + epics.md + UX设计规格说明.md + 技术架构详细设计.md 为准`
